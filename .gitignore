# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.env.development.local

# Supabase CLI local state (project ref, pooler URL, etc - machine-specific)
supabase/.temp

# Any dotfile ending in .env, whatever it is called. A credentials file was
# committed here once as `.workos-delpoy.env` - a typo of "deploy" - which slid
# straight past a rule naming the correct spelling. Match the shape, not the
# name, because the name is the part that gets typed wrong.
*.env
!.env.example
