# The only variable the frontend reads. Everything else (service-role key,
# JWT signing secrets, storage credentials) lives server-side as Edge Function
# secrets and must never be exposed to the browser bundle - see DEVDOC.md.
#
# Base URL of your Supabase project; the app calls {this}/functions/v1/workos
VITE_SUPABASE_URL=
